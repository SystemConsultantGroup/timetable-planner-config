apiVersion: v1
kind: Service
metadata:
  name: timetable-planner-redis-service
spec:
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app.kubernetes.io/name: timetable-planner-redis